/**
 * @return {Generator<number>}
 */
var fibGenerator = function(n) {    
    let fib = 0;
    let count = 0;
    let temp = 0;

    while(count < n) {
        temp = fisrt + second;
        console.log(temp);
        fisrt = second;
        second = temp;;        
        count++;
    }
};


const gen = fibGenerator(5);
 